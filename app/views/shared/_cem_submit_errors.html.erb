<% if @cemetery.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@cemetery.errors.count, "error") %>.
    </div>
    <ul>
    <% @cemetery.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
