<% provide(:title, 'All users') %>
<div class="inner-container">
  <h1>All users</h1>
  <%= render :partial => 'shared/results_range', :locals => {:object => @users} %>
  <%= will_paginate(@users, :params => params.except(:page)) %>
  <ul class="users">
    <%= render @users %>
  </ul>
  <%= will_paginate(@users, :params => params.except(:page)) %>
</div>
